class SpeechManager { constructor() { this.recognition = null; this.status = 'idle'; this.onListeningStateChange = null; this.onError = null; this.init(); } isSupported() { return !!(window.SpeechRecognition || window.webkitSpeechRecognition); } init() { if (!this.isSupported()) return; const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition; this.recognition = new SpeechRecognition(); this.recognition.lang = 'fr-FR'; this.recognition.continuous = false; this.recognition.interimResults = true; this.recognition.onstart = () => { this.status = 'listening'; if (this.onListeningStateChange) this.onListeningStateChange(true); }; this.recognition.onend = () => { this.status = 'idle'; if (this.onListeningStateChange) this.onListeningStateChange(false); }; this.recognition.onerror = (e) => { if (this.onError) this.onError(e.error); }; } startListening(onResult) { if (!this.recognition) return; this.recognition.onresult = (e) => { let final = ''; for (let i = e.resultIndex; i < e.results.length; ++i) if (e.results[i].isFinal) final += e.results[i][0].transcript; onResult(final, !!final); }; this.recognition.start(); } stopListening() { if (this.recognition) this.recognition.stop(); } isListening() { return this.status === 'listening'; } }
